spring:
  application:
    name: task-service

server:
  port: 8080
  contextPath: /task-service


eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8302/eureka/
  instance:
    hostname: localhost